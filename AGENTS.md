# AGENTS.md — 足球密码网站执行规则

本文件给 Codex / 自动化 Agent 使用。

## 1. 仓库身份

这是「足球密码 FOOTBALL CODE」网站执行仓库。

上游规则与商品事实总部：`msjddnjsjdbd-stack/taobao-visual-system`。

## 2. 执行优先级

1. 用户当次明确要求
2. 总部对应商品任务卡与已确认商品事实
3. 总部 `products/catalog.json` 稳定商品目录
4. 总部 `assets/manifest.json` 文件资产清单
5. 总部当前视觉铁律与品牌规则
6. 本仓库 `data/products.json`
7. 现有页面代码与历史实现

如果信息冲突，不要猜。先保留现状并指出冲突。

## 3. 商品事实红线

- 不编造品牌、联名、授权、功能、材质、尺码、颜色、库存、促销或售后承诺。
- 不因为图片看起来像某种功能，就把它写成正式卖点。
- 未确认“高弹、速干、防水、防泼水、保暖、轻量”等功能时，不得擅自添加。
- 价格只能来自用户明确确认或总部已确认资料。
- 没有真实购买链接时，不创建虚假购买跳转。
- `website-image` 只代表网站展示资产，不自动成为商品真实原图/结构证据。

## 4. 数据与页面

- 商品展示数据唯一入口：`data/products.json`。
- 总部稳定字段来源：`taobao-visual-system/products/catalog.json`。
- 总部正式资产索引：`taobao-visual-system/assets/manifest.json`。
- 总部 → 网站字段映射规则：`taobao-visual-system/docs/product-to-web-handoff.md`。
- 网站商品记录应尽量保留 `sourceId`，用于回查总部商品目录与资产清单。
- `index.html` 负责读取数据并渲染页面；不要再把商品价格和描述复制成第二份硬编码数据。
- 商品图片放在 `images/`，路径写入 `data/products.json`。
- 新增商品时优先追加数据，不为每个商品复制整块页面代码。
- 更换网站正式商品图时，应同步检查/更新总部资产清单中的对应 `website-image` 记录，避免网站和总部失去追踪关系。

## 5. 网站视觉

- 品牌名称统一为「足球密码 FOOTBALL CODE」。
- 视觉方向：足球、运动、年轻、清晰、高级，避免廉价促销堆叠。
- 商品本身优先于装饰。
- 移动端必须可正常浏览，商品卡不得横向溢出。
- 不随意修改用户已确认商品图片。
- 如果总部存在 `approved-output`，优先复用明确适用于网站的已确认成品，不从参考图自行重造商品结构。

## 6. 修改流程

每次网站任务：

1. 先检查总部对应任务卡、`products/catalog.json`、`assets/manifest.json` 和交接协议。
2. 再检查当前 `index.html`、`data/products.json` 与相关图片。
3. 确认本次变更所需商品事实，核对 `sourceId`、价格、名称、图片、资产身份和购买链接。
4. 在独立分支完成最小改动。
5. 检查 HTML/JSON 语法与相对路径。
6. 检查手机端响应式布局。
7. 如更换正式网站图片，同步维护总部资产追踪记录。
8. 说明改了什么、没改什么。
9. 通过 PR 合并到 `main`。

## 7. 与总部的边界

总部负责：
- 商品事实
- 稳定商品目录 `products/catalog.json`
- 文件资产清单 `assets/manifest.json`
- 品牌资产与视觉铁律
- 主图/详情页规范
- 商品任务卡
- QC 标准

本仓库负责：
- 页面代码
- 网站商品展示数据
- 网站实际图片文件
- 实际上线内容

网站图片可以由总部资产清单跨仓库登记，但不需要为了“集中管理”而重复复制同一张二进制图片。

不要把总部整套规则复制进本仓库；只在需要时读取上游规则。

## 8. 当前技术边界

当前网站是静态 HTML/CSS/JS。

在没有明确需求前：
- 不擅自引入大型框架
- 不增加复杂构建系统
- 不引入需要密钥的第三方服务
- 不改变托管方式

优先保持简单、稳定、容易维护。
